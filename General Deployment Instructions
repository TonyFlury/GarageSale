1) Make sure all changed files are committed and pushed.
    Ensure this includes :
        Migration files
        js and css
        static images
        python files

..bash:
    cd /Development/GarageSaleClone
    git clone https://github.com/TonyFlury/BranthamGarageSale.git
    git checkout master
    catalog check

2) Open a bash console on Pythonanywhere

..bash:
        cd BranthamGarageSale or cd TestBranthamGarageSale
        git pull origin master
        catalog check

        python manage.py collectstatic

        python manage.py migrate

if you have a new databse :
    python manage.py loaddata ContentTypes.json
    python manage.py loaddata Permissions.json
    python manage.py loaddata Groups.json
    python manage.py loaddata transaction_categories.json
    python manage.py loaddata TemplateData.json

3) Credentials files :
    Using the files app on the python anywhere website
    Copy the appropriate files to the credentials folder

4) On python anywhere Dashboard
        in wsgi.py
        define the environment variables :
        DJANGO_GOOGLE_OAUTH_CLIENT_ID
        and
        DJANGO_GOOGLE_OAUTH_CLIENT_SECRET

        values are from the google cloud console for the live test or deployment clients.
        Different values for test and live and development

        Restart Server.
